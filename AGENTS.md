# Ikki 开发约束

- 代码中保留必要注释，优先解释设计意图、边界条件和阶段性占位原因。
- 不为显而易见的语句添加注释，避免把注释写成代码复述。
- 注释、docstring、README、开发文档和面向用户的 CLI 文案默认使用中文。
- Python 标识符继续使用常规英文命名，保证生态兼容性和可读性。
- 编程时尽可能采用面向对象的方式组织代码，优先用清晰的对象边界表达职责。
- 在设计方案或遇到需要取舍的关键决策时，先向用户说明选项和影响，并等待用户选择，不要自行定案。
- 创建开发分支时不要使用 `codex/` 作为分支名前缀。
- 每个阶段完成后，同步更新 `README.md` 和 `docs/milestones.md`。
